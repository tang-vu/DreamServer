name: Garage extension
on:
  pull_request:
    paths:
      - 'ods/extensions/library/services/garage/**'
      - 'ods/tests/test_garage.py'
      - '.github/workflows/garage-extension.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install pytest boto3==1.40.40
      - run: python -m pytest ods/tests/test_garage.py -q
  live:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install pytest boto3==1.40.40
      - run: sudo --preserve-env=PATH env ODS_TEST_GARAGE=1 python -m pytest ods/tests/test_garage.py -q -s
