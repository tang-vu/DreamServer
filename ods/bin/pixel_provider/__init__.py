"""Pixel-scoped provider and transition contracts.

These modules do not grant execution privileges or change ODS's global model
route. Runtime activation is an explicit, separately verified operation.
"""
